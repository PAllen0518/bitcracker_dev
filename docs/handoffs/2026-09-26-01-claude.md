# Checkpoint: Checkpoint-integrity spec drafted (gate 1, awaiting approval)

## 1. Metadata

- Checkpoint ID: `2026-09-26-01-claude`
- Written (America/Denver): `2026-09-26 15:10`
- Author agent: `Claude`
- Git HEAD: `a01d2b6` on worktree `claude/checkpoint-integrity-spec`
  (path `../BitCracker_V2-wt-checkpoint-spec`, branched from `a01d2b6`)
- Working tree: `dirty` — two new untracked docs in the worktree (see §4). No
  tracked code changed. Nothing staged/committed.
- Workspace DB run ID: `n/a` (coordination workspace not built)

## 2. What was done this session

- Read the governance set before acting: `COLLABORATION.md`, `AGENTS.md`,
  `CLAUDE.md`, `docs/handoffs/TEMPLATE.md`.
- Verified the current save format directly (not assumed). Evidence:
  - `SaveState` @ `multibit_cuda_threads.cu:913-921`, `sizeof = 1064`,
    `OLD_SAVE_STATE_SIZE = 1048`. No magic/version/hash/checksum.
  - `save_progress:927-930` — `fopen`/`fwrite`/`fclose`, no return checks, no
    flush/fsync, no temp+rename → not atomic/durable.
  - Restore `:1101-1113` — only `total_combos` compared; no binding of
    token-list bytes, wallet identity, delimiter, or typo flags.
  - Wallet identity source `load_wallet:436-455` → `h_salt[8]`+`h_enc[32]`.
- Confirmed the Search60 constraint. Evidence: `stat` reports
  `cuda_threads_save60.bin` = **1064 bytes** (current struct format; contents
  NOT read). Five `*.bak-*` copies are also 1064 bytes.
- Wrote the spec draft `docs/specs/CHECKPOINT_INTEGRITY_SPEC.md` (v1 format with
  identity bindings + checksum, fail-loud restore, atomic durable writes,
  confirmed legacy re-bind migration that preserves the 76%, host-side test
  plan with named RED tests). Draft only — not committed.

## 3. Recovery state (no secrets)

- Active search: `Search60`
- Progress: `~76%` (per prior Codex inspection; not re-derived this session)
- Passwords checked: `n/a this session`
- Checkpoint file: `cuda_threads_save60.bin`, size 1064 bytes, sha256 not
  recorded here (contents not read); last write not inspected
- Token-list file: `search60.txt`, sha256 not recorded (contents NOT reproduced)
- Recovery output present: `not checked this session`
- Recovery process running: `no` (paused run per prior handoff)
- Classification: `paused/incomplete`

## 4. Changed / relevant files

- `docs/specs/CHECKPOINT_INTEGRITY_SPEC.md` — new, the deliverable spec (draft).
- `docs/handoffs/2026-09-26-01-claude.md` — this handoff.
- No tracked code touched. `multibit_cuda_threads.cu` read-only this session.
- Do NOT list or move untracked recovery artifacts (see §7).

## 5. Open approval gates (human-only)

- [ ] Gate: `Specification approval` — `waiting` — **blocks: all implementation.**
      This is the hard stop. No RED/GREEN until Paul approves the spec.
- [ ] Gate: `Confirm Search60 truly completed` — `waiting` — unrelated to this
      spec, but note the run is paused/incomplete, not failed.

## 6. Next actions for the picking-up agent

1. Do NOT write code. Spec §8 questions are resolved (Paul, 2026-09-26):
   interactive typed `REBIND` confirm; retain `<path>.prev` on every save; add
   `SAVE_TOOL_VERSION`. Gate 1 (spec approval) is still the hard stop.
2. Only after Paul approves the spec: begin RED — extract `save_format.hpp`,
   write the named host tests, and record failing output before any fix.
3. Never use the real `cuda_threads_save60.bin` as a test input; use synthetic
   1064-/1048-byte blobs.

## 7. Blockers and warnings

- Untracked artifacts that must NOT enter any worktree or commit:
  `search60.txt`, `cuda_threads_save60.bin` (+ `*.bak-*`), `search60*.log`, local
  `.gitignore` change. They are absent from this worktree; keep it that way.
- Known bug being fixed: restore validates combo count only; saves are not
  atomic. A resumed run against a changed token list / wallet / delimiter / typo
  set with an equal combo count would silently search the wrong space.
- Migration risk: the 76% save is a 1064-byte legacy record. Any implementation
  must read it at the documented offsets and must not overwrite it until a
  complete flushed v1 file exists (the spec's `.legacy` + atomic-rename rule).

## 8. Verification the next agent must run

- `git -C "C:/Users/Paul Allen/Documents/BitCracker_V2-wt-checkpoint-spec" status --short --branch`
  (expect the two new docs untracked, no code changes).
- `git -C "C:/Users/Paul Allen/Documents/BitCracker_V2-wt-checkpoint-spec" log -1 --format='%H %s'`
  (expect `a01d2b6 docs(cuda): ...`).
- Re-confirm `stat -c '%s' cuda_threads_save60.bin` == 1064 before trusting the
  migration case in the spec.
- Re-read `multibit_cuda_threads.cu:913-937` and `:1101-1113` to confirm the save
  layout and restore check are unchanged since this checkpoint.
- If any code changed since this checkpoint, run `python -m pytest tests/ -v`.
