# Checkpoint: Save-integrity implemented (RED→GREEN→gauntlet), awaiting human gates

## 1. Metadata

- Checkpoint ID: `2026-09-26-02-claude`
- Written (America/Denver): `2026-09-26 15:40`
- Author agent: `Claude`
- Git HEAD: `a01d2b6` on worktree `claude/checkpoint-integrity-spec`
  (path `../BitCracker_V2-wt-checkpoint-spec`)
- Working tree: `dirty` — 4 tracked files modified, 1 new tracked-worthy header,
  new docs (see §4). Nothing staged or committed.
- Workspace DB run ID: `n/a`

## 2. What was done this session

- Gate 1 (spec) approved by Paul with the 3 §8 decisions folded in.
- Extracted the save format into `save_format.hpp` (behavior-preserving) and
  implemented the v1 format + fail-loud restore + confirmed legacy migration +
  atomic durable writes with `<path>.prev`. Full detail + commands + hashes in
  `docs/specs/CHECKPOINT_INTEGRITY_EVIDENCE.md`.
- RED: 7/14 new contracts failed with validation + atomic writer stubbed
  (`.cuda-build/checkpoint-red.txt`). GREEN: 14/14 pass
  (`.cuda-build/checkpoint-green.txt`).
- Gate-5 coverage pass: added 4 branch contracts (badmagic, badversion,
  combocount, detect-unknown) so all 9 `validate_save` branches + Unknown are
  covered; public-fixture/secret audit clean. See the evidence "Coverage &
  public-fixture validation" section.
- Gauntlet: full suite **140 passed, 0 skipped**; both C++ builds exit 0, no
  warnings; Ruff clean; end-to-end CLI fail-loud + migration tests pass.

## 3. Recovery state (no secrets)

- Active search: `Search60` — `paused/incomplete`, ~76%, 1064-byte legacy save.
- This session did not run, resume, read, or modify `cuda_threads_save60.bin` or
  any recovery artifact. Migration was proven on synthetic fixtures only.
- `RECOVERED_PASSWORD.txt`: not created by any test (tests assert its absence in
  no-recovery cases).

## 4. Changed / relevant files (all in the worktree, uncommitted)

- `BitCracker/btcrecover-master/save_format.hpp` — NEW: legacy + v1 save logic.
- `BitCracker/btcrecover-master/multibit_cuda_threads.cu` — include header;
  fail-loud restore, confirmed migration, v1 atomic autosave.
- `BitCracker/btcrecover-master/tests/cuda_host_contracts.hpp` — 14 contracts.
- `BitCracker/btcrecover-master/tests/test_cuda.py` — register the contracts.
- `BitCracker/btcrecover-master/tests/test_cuda_cli.py` — v1 layout + 4 new
  fail-loud/migration CLI tests.
- `docs/specs/CHECKPOINT_INTEGRITY_SPEC.md`, `docs/specs/CHECKPOINT_INTEGRITY_EVIDENCE.md`,
  this handoff.

## 5. Open approval gates (human-only)

- [x] Gate: `Specification approval` — cleared this session.
- [ ] Gate: `Public-fixture & coverage validation` — `validation ran, awaiting
      Paul's sign-off` — blocks commit. Material: evidence "Coverage &
      public-fixture validation" section (audit clean; all 9 validate_save
      branches + detect Unknown covered; 140 passed).
- [ ] Gate: `User-owned commit and push` — `waiting` — **blocks landing.**

## 6. Next actions for the picking-up agent / Paul

1. Review `save_format.hpp` and the `main()` restore/autosave wiring.
2. If satisfied, Paul commits (agents must not). Suggested set (excludes
   `.cuda-build/` and recovery artifacts), from the repo root:
   ```
   git add -- BitCracker/btcrecover-master/save_format.hpp \
     BitCracker/btcrecover-master/multibit_cuda_threads.cu \
     BitCracker/btcrecover-master/tests/cuda_host_contracts.hpp \
     BitCracker/btcrecover-master/tests/test_cuda.py \
     BitCracker/btcrecover-master/tests/test_cuda_cli.py \
     docs/specs/CHECKPOINT_INTEGRITY_SPEC.md \
     docs/specs/CHECKPOINT_INTEGRITY_EVIDENCE.md \
     docs/handoffs/2026-09-26-01-claude.md docs/handoffs/2026-09-26-02-claude.md
   git commit -m "feat(cuda): bind, verify, and atomically write checkpoints"
   ```
3. To migrate Search60 later: back it up, then run the built app with
   `--restore cuda_threads_save60.bin` **interactively** and type `REBIND` when
   prompted (it will not proceed on redirected/empty stdin). It writes
   `cuda_threads_save60.bin.legacy` before the first v1 write.

## 7. Blockers and warnings

- Do NOT commit `BitCracker/btcrecover-master/.cuda-build/` (build output; shows
  as untracked in this worktree) or any recovery artifact.
- The save format changed (v1, 1216 bytes, magic `MBCKSV1\0`). Any external tool
  that parsed the old 1064-byte layout must be updated; the only in-repo consumer
  (`tests/test_cuda_cli.py`) already was.
- `docs/` and the standards files may be untracked in the base commit; confirm
  what is actually tracked before committing.

## 8. Verification the next agent must run

- `git -C "C:/Users/Paul Allen/Documents/BitCracker_V2-wt-checkpoint-spec" status --short`
- `cd BitCracker/btcrecover-master && python tools/build_cuda.py optimized_test && python tools/build_cuda.py optimized`
- `python -m pytest tests/ -q` (expect 140 passed, 0 skipped).
- Re-hash the five source files; confirm they match the evidence report's
  "Final source identity" section.
- Confirm `cuda_threads_save60.bin` is still 1064 bytes and untouched.
