# Settings
.clangd
*_properties.json
settings.json
*.zip

# CLAUDE
.claude
*.pyc
*.pyo
/addresses.db
claude.md

# Python tooling caches
__pycache__/
.pytest_cache/
.ruff_cache/

# Sensitive - wallet key files
*.key
# ...except btcrecover's public test fixture (password "btcr-test-password"),
# which the test suite needs and CI checks out fresh.
!**/test-wallets/multibit-wallet.key

# Sensitive - personal password/token search lists
search*.txt
token*.txt
Testsearch.txt

# Sensitive - search notes containing candidate passwords (any naming variant)
*[Ss]earch*[Nn]otes*.txt

# Autosave/restore files from btcrecover and GPU recovery runs
savefile*
*.pkl
*_progress_log.csv

# Internal engineering audit (not for publication yet)
audit/

# Recovered passwords, written to a file instead of stdout (never commit these)
RECOVERED_PASSWORD.txt
# CUDA Build Artifacts
*.exe
*.bin
*.obj
cl_intercept.log
nvcc_*.txt